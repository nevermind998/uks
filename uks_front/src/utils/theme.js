import { createTheme } from '@mui/material';

const theme = createTheme({
  palette: {
    primary: {
      main: '#393d3f',
    },
    secondary: {
      main: '#bfc0c0',
    },
  },
});

export default theme;
